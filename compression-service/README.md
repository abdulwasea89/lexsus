# compression-service/ — LLM Compression Microservice

Local Python + FastAPI service for LLM-based context compression (Layer 3).

**Status: placeholder.** This directory is empty and will be populated when the service is scaffolded.

## Planned contents
- `main.py` — FastAPI app, Served on localhost:8000
- `requirements.txt` — dependencies (FastAPI, uvicorn, LangChain)
- Runs via Docker (see root `Dockerfile` + `docker-compose.yml`)

_See `docs/architecture.md` and `docs/tech-stack.md` for the full design._
